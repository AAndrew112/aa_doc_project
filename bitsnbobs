$files = Get-Content "C:\path\to\filelist.txt"
$source = "C:\path\to\source"
$dest = "C:\path\to\output"

foreach ($f in $files) {
    $match = Get-ChildItem -Path $source -Recurse -File | Where-Object { $_.Name -eq $f }
    if ($match) { Copy-Item $match.FullName -Destination $dest }
}
